{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/itrujnara/featureextractor/master/assets/schema_input.json",
    "title": "itrujnara/featureextractor pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fasta": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?a(\\.gz)?$",
                "errorMessage": "FASTA file must be provided and cannot contain spaces and must have extension '.fa' or '.fasta', possibly gzipped."
            },
            "gff": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.gff(3)?(\\.gz)?$",
                "errorMessage": "GFF file must be provided and cannot contain spaces and must have extension '.gff' or '.gff3', possibly gzipped."
            }
        },
        "required": ["id", "fasta", "gff"]
    }
}
